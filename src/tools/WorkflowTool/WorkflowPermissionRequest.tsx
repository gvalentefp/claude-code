import React from 'react'
import { Box, Text } from '../../ink.js'

export function WorkflowPermissionRequest(): React.JSX.Element {
  return (
    <Box>
      <Text>Workflow permissions are unavailable in this local build.</Text>
    </Box>
  )
}
