import React from 'react'
import { Box, Text } from '../../ink.js'

export function TungstenLiveMonitor(): React.JSX.Element {
  return (
    <Box>
      <Text>Tungsten monitor is unavailable in this local build.</Text>
    </Box>
  )
}
